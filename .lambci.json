{
  "cmd": "pip install --user tox && tox"
}
{
  branches: {
    master: true,
    version1: {
      build: true,
      notifications: {
        slack: {
          channel: '#dev'
        }
      }
    }
  }
}
